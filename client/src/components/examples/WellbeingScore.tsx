import { WellbeingScore } from '../WellbeingScore';

export default function WellbeingScoreExample() {
  return (
    <div className="p-6 max-w-md mx-auto">
      <WellbeingScore score={82} />
    </div>
  );
}
